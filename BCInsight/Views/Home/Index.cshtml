@using BCInsight.Web.HelperClass
@{
    ViewBag.Title = "Home Page";
    var currentUser = Utility.CurrentLoginUser;
    if (currentUser == null)
    {
        Response.Redirect(Url.Action("Index", "Login", new { Area = "" }));
    }
}

<!-- Content Header (Page header) -->
<div class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-10">
                
            </div><!-- /.col -->
            <div class="col-sm-2">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="@Url.Action("")">Home</a></li>
                    @*<li class="breadcrumb-item active">Dashboard v2</li>*@
                </ol>
            </div><!-- /.col -->
        </div><!-- /.row -->
    </div><!-- /.container-fluid -->
</div>
<!-- /.content-header -->

    <div class="card card-primary card-tabs">
        <div class="card-body">
            <div class="row">
                <div class="col-md-6 text-center">
                    <h1 class="m-0">@ViewBag.laststock <span style="font-size:15Px">@*@Model.LastUpdated*@</span></h1>  
                </div>
                <div class="col-md-6 text-center">
                    <h1 class="m-0">@ViewBag.lastsale <span style="font-size:15Px">@*@Model.LastUpdated*@</span></h1> 
                </div>
            </div>
        </div>
    </div>

